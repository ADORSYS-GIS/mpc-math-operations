apiVersion: v1
kind: Service
metadata:
  name: {{ include "mcp-math-operations.fullname" . }}
  labels:
    {{- include "mcp-math-operations.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "mcp-math-operations.selectorLabels" . | nindent 4 }}
